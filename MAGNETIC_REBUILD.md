# 磁吸条文重建（MVP）

## 产品边界

本功能是 SQLite/DB 原子题库新增的独立出题模式，不替换、不修改：

- 常规练习、考试、错题、收藏、历史和结果统计；
- 现有填空题五种出题模式；
- 自适应渐隐练习；
- 原始题库和原子答案数据；
- 现有答题页 UI。

## 使用入口

主页点击原子题库卡片，在“选择学习方式”中选择“磁吸重建”。设置 → 出题模式 → 原子题库出题模式中增加对应说明项。

## MVP 玩法

1. 从原子填空题恢复完整条文；
2. 把原文拆成 3–12 个保持原始顺序的语义词块；
3. 用户点击候选词块加入组装区；
4. 长按左右拖动已放置词块改变顺序；
5. 正确相邻关系点亮“磁吸回路”；
6. 支持撤销、提示、查看原文、重新打乱和检查；
7. 完成后进入下一条，最后显示本轮总结；
8. 自动保存当前题号、已完成条文和当前词块布局，退出后再次进入可继续上次进度。

## 架构

- `QuestionSessionKind.MagneticRebuild`：独立 Session 类型；
- `MagneticRebuildQuestionPipeline`：原文恢复、语义块生成和稳定打乱；
- `MagneticRebuildSession`：唯一行为状态边界，通过 `SessionCommand.Magnetic*` 接收 UI 操作；
- `MagneticRebuildScreen`：独立 Compose UI，不复用或修改现有 PracticeScreen；
- 原题库只读，不写正式练习历史和错题；磁吸进度使用独立 `magnetic_` 作用域保存。

## 限制

- 仅显示 3–12 个可用语义块的原子填空条文；
- 超长条文的层级拼图和跨日熟练度调度属于后续阶段；
- 拼图完成表示本轮结构恢复成功，不等同于长期掌握。

## 词块碎化程度

设置页提供五档粒度：大块（最多6块）、偏粗（9块）、标准（12块）、偏细（16块）、细碎（24块）。算法从原子级片段开始，按语义边界成本逐步合并，优先避免跨句号、逗号和分项边界形成生硬组合。

每次重新进入磁吸重建时，以设置页最新粒度为准。粒度未变化时完整恢复各题草稿；粒度变化时保留题序、当前题号和已完成条文，仅清除不兼容的未完成词块布局并按新粒度重新生成。

## 重复词块等价规则

同一条文中若出现多个标准化文本完全相同的词块，例如两处“电压等级。”，这些词块在正确位置上可以互换。系统会在每次加入、撤回或拖动后，仅交换这些重复词块的内部位置身份，不改变用户看到的文字和排列。

唯一 ID 仍只负责拖动、撤回和进度恢复；不同文本即使语义相近仍不能互换。

## 紧凑任务区与答题卡

磁吸页面将原“条文重建任务”大卡片压缩为紧凑状态卡，只保留模式、完成数、磁吸回路和进度条。标题栏右侧提供与练习页一致的答题卡入口：

- 可直接跳到任意条文，未操作的当前条文可以跳过；
- 已开始但未完成的条文以“已开始”状态显示；
- 已完成条文单独标记，完成数不再依赖顺序；
- 每一条未完成的候选词块、已组装词块和计数独立保存，跳走后再返回仍能继续；
- 完成当前条文后，“下一未完成”会自动寻找后续未完成条文，全部完成才结束本轮。

进度存储格式升级为 `magnetic-rebuild-v3`。旧 `v1/v2` 的题序、当前题号和已完成条文继续保留；旧词块草稿因分词与标点归属规则变化会安全失效并重新生成。

## 标点归属规则

- 句号、逗号、顿号、分号、冒号、问号、感叹号及右括号等右侧标点，固定附着到上一语义块；
- `（一）`、`（二）` 等分项序号固定作为下一语义块的开头；
- 碎化合并不得跨越句末或分项序号边界，即使因此略微超过目标最大词块数，也优先保证条文结构正确；
- 不再生成“。 （三）具备……”或“：（一）经……”这类标点跑到下一词块前面的内容。
