---
description: 架构守门 — PowerAI Engineering OS Guard 细则（全项目 LOC/管道/拆分）
alwaysApply: true
---

# Architecture Guard（强制 · 全项目生效）

> **与 `.ai/workflows/architecture_guard.md` 同步。**  
> 入口：`.ai/README.md` | 宪法：`.ai/PROJECT_CONSTITUTION.md`  
> **适用范围**：整个 Gradle 多模块工程（`:app`、`:feature-*`、`:data`、`:domain`、`:core`、`:ui-common` 等）。

---

## 【架构强制 — 先于写代码执行】

### 1. 必读上下文（按触达范围递增）

| 优先级 | 文件 | 何时读 |
|--------|------|--------|
| 必 read | `.ai/workflows/architecture_guard.md` | **任何**改代码任务 |
| 必 read | `.ai/current/loc_audit.md` | **任何**改代码任务 |
| 必 read | `.ai/current/current_state.md` | **任何**改代码任务 |
| 按需 | `.ai/practice_screen_decomposition.md` | 动 `PracticeScreen` / `PracticeScreen*.kt` |
| 按需 | `.ai/exam_screen_decomposition.md` | 动 `ExamScreenContent` / `ExamScreen*.kt` |
| 按需 | `.ai/practice_viewmodel_decomposition.md` | 动 `PracticeViewModel` |
| 按需 | `.ai/navigation_controller_decomposition.md` | 动练习导航 |
| 按需 | `.ai/current/module_map.md` | 不确定模块归属时 |
| 按需 | `.ai/rules/session_rules.md` | 动 Session / Host / Registry |
| 按需 | `.ai/rules/compose_rules.md` | 动 Screen / Route |

### 2. Architecture Placement Review（改前必做）

见 `.ai/workflows/architecture_guard.md` Step 5 模板。

### 3. 设计原则（五条）

见 `.ai/architecture/coding_principles.md`。

### 4. LOC 红线（全项目 · 改前估算）

见 `.ai/architecture/coding_principles.md` 表格。  
**全仓库硬扫描**：`scripts/check-loc-over-500.ps1` → `.ai/current/loc_audit.md`。

### 5. 禁止（全项目）

见 `.ai/rules/cursor_rules.md` + `.ai/PROJECT_CONSTITUTION.md`。

### 6. 完成后验证与文档

```powershell
scripts/check-loc-over-500.ps1
# 按需
scripts/check-practice-screen-loc.ps1
scripts/check-exam-screen-loc.ps1
```

文档更新（有拆分或 LOC 变化时）：
- `.ai/current/change_log.md`
- `.ai/current/loc_audit.md`
- `.ai/*_decomposition.md`

---

## 新 UI / 新逻辑放哪

见 `.ai/architecture/coding_principles.md` 与 `.ai/architecture/module_rules.md`。

---

## 用户可复制的一句话指令

```
【PowerAI Engineering OS】
1. 读 PROJECT_CONSTITUTION + README + current_state + loc_audit
2. 执行 workflows/architecture_guard.md 六步，输出 PASS
3. 按 task_execution.md 出 Plan 再编码
4. 完成后 check-loc-over-500.ps1，更新 current/change_log 与 loc_audit
```
